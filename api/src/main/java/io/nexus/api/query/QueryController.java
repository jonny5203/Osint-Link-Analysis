package io.nexus.api.query;

import org.springframework.stereotype.Controller;

/** GraphQL query resolvers (PLAN.md T7.4): search, neighbors, shortestPath. */
@Controller
public class QueryController {
    // TODO(T7.4): @QueryMapping for search / neighbors / shortestPath
}
