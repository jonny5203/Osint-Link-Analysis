// PLAN.md T10.3: "New Investigation" -> CREATE_INVESTIGATION,
// "Save Layout" -> serialize positions -> SAVE_LAYOUT, list of saved investigations to reload.
export function InvestigationPanel() {
  return (
    <aside className="investigation-panel">Investigation (T10)</aside>
  );
}
