// PLAN.md T9.6 (stub) / T10.2 (flesh out).
// T9.6: when a node is selected, show id + name only.
// T10.2: show all properties, sanction programs, sources, ALSO_KNOWN_AS cluster, annotation input.
export function NodeDetailPanel() {
  return (
    <aside className="node-detail">Node detail (T9/T10)</aside>
  );
}
